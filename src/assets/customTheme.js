import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  direction: "rtl",
  palette: {
    primary: {
      main: "#000",
    },
    secondary: {
      main: "#FFF",
    },
  },
  typography: {
    fontFamily: "iransans",
  },
});
